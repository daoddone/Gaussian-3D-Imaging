"""Stage 3: metric alignment and validation (custom, model-agnostic).

Importable as a package (tests do ``from stages.stage3_metric import align``) and
also runnable as a script (``python stages/stage3_metric/run.py``). The sibling
modules use a try/except import so both invocation styles work.
"""
