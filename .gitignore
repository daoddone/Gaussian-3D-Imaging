# Working data: one subfolder per capture. Excluded from version control per Section 3.5.
sessions/

# Third-party model repos cloned locally into the tree (installed per stage, not vendored).
Depth-Anything-3/
Free-Geometry/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Conda / environment scratch
*.conda
*.tar.bz2

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Model weights and large caches (downloaded on first use, never committed)
*.ckpt
*.safetensors
*.pth
weights/
.cache/

# iOS / Xcode (Stage 1 app). The .xcodeproj is generated from project.yml by
# XcodeGen; project.yml is the checked-in source of truth.
*.xcodeproj/
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.ipa
.swiftpm/
.swift-version
swiftly.pkg

# sweetpad / xcode-build-server integration for VS Code (regenerated per machine)
buildServer.json